# 🚀 SysAdmin Portfolio - nemesino-png

**Infra PRODUCCIÓN LIVE en mi máquina**

## Comandos que DEMUESTRAN skills:
```bash
curl localhost           # Nginx nativo ✓
curl localhost:8080      # Nginx Docker ✓  
ls ~/backups/            # 873M backups ✓
tail ~/backup.log        # Logs cron ✓
crontab -l               # Backup 2AM ✓
~/backup.sh              # Script LIVE ✓
